# Tamagotchi Crypto Wallet

Модульный криптокошелёк нового поколения с виртуальным питомцем. Проект демонстрирует концепцию расширяемого криптокошелька, состоящего из независимых модулей, которые взаимодействуют через единую систему событий.

## Концепция проекта

Традиционные криптокошельки — это монолитные приложения без души и кастомизации. **Tamagotchi Crypto Wallet** представляет альтернативу:

- **Модульность** — кошелёк состоит из независимых модулей (кошелёк, тамагочи, и потенциально другие)
- **Связность** — модули взаимодействуют через Event Bus, реагируя на действия друг друга
- **Кастомизация** — виртуальный питомец может быть уникально настроен под вкусы пользователя

## Модули

### Модуль кошелька (Wallet Module)

Полнофункциональный криптокошелёк для работы с Ethereum блокчейном:

- **Создание и импорт кошельков** — генерация HD-кошельков по стандартам BIP-39/BIP-44
- **Безопасное хранение** — AES-256 шифрование приватных ключей и мнемоники
- **Работа с блокчейном** — отправка и получение ETH, просмотр истории транзакций
- **Сеть Sepolia** — работа с тестовой сетью Ethereum

**Подробная документация:** [WALLET_MODULE.md](./WALLET_MODULE.md)

### Модуль Тамагочи (Tamagotchi Module)

Виртуальный питомец, который реагирует на действия с кошельком:

- **Управление питомцем** — кормление, развлечения, отслеживание счастья и сытости
- **Кастомизация** — покупка и выбор питомцев, шляп, ботинок
- **Интеграция с кошельком** — получение наград за транзакции и создание кошелька
- **Автоматическое уменьшение статов** — счастье и сытость уменьшаются со временем

**Подробная документация:** [TAMAGOTCHI_MODULE.md](./TAMAGOTCHI_MODULE.md)

## Связность модулей

Модули взаимодействуют через **Event Bus**:

```
Кошелёк → Event Bus → Тамагочи
```

**События кошелька:**
- `wallet:created` — создание кошелька → +50💰, +20 счастья
- `wallet:transaction-sent` — отправка транзакции → +10💰, +10 счастья
- `wallet:transaction-received` — получение транзакции → +15💰, +15 счастья
- `wallet:balance-changed` — изменение баланса

При событиях кошелька отображается анимация летающих банкнот в модуле тамагочи.

## Быстрый старт

### Требования

- Node.js 18+ и npm
- API ключи (опционально, для полной функциональности кошелька)

### Установка

```bash
# Клонировать репозиторий
git clone <repository-url>
cd tamagotchi_crypto_wallet

# Установить зависимости
npm install
```

### Конфигурация

Создайте файл `.env` в корне проекта:

```env
# Infura RPC URL для подключения к Sepolia (опционально)
VITE_INFURA_RPC_URL=https://sepolia.infura.io/v3/YOUR_INFURA_API_KEY

# Etherscan API ключ для истории транзакций (опционально)
VITE_ETHERSCAN_API_KEY=YOUR_ETHERSCAN_API_KEY
```

**Получение ключей:**
- **Infura**: https://infura.io/ (бесплатный аккаунт)
- **Etherscan**: https://etherscan.io/apis (бесплатный API ключ)

> **Примечание:** Без API ключей модуль кошелька будет работать с ограниченной функциональностью (без истории транзакций). Модуль тамагочи работает полностью независимо.

### Запуск

```bash
# Запуск dev-сервера
npm run dev
```

Приложение откроется по адресу `http://localhost:5173` (или другому порту, указанному Vite).

### Сборка для production

```bash
npm run build
npm run preview
```

## Использование

### Модуль кошелька

1. **Создание кошелька** — сгенерируйте новый кошелёк или импортируйте существующий по мнемонической фразе
2. **Разблокировка** — введите пароль для доступа к кошельку
3. **Отправка ETH** — отправляйте транзакции на Sepolia testnet
4. **История транзакций** — просматривайте все транзакции через Etherscan API

### Модуль тамагочи

1. **Главная страница** — просмотр питомца и статистики (счастье, сытость)
2. **Магазин** — покупка питомцев, шляп и ботинок за игровую валюту
3. **Кормление** — восстановление сытости питомца
4. **Развлечения** — восстановление счастья питомца

### Админские команды

В консоли браузера доступны команды для отладки:

```javascript
// Деньги
admin.addMoney(1000)
admin.setMoney(500)

// Время
admin.fastForwardTime(30)  // Прокрутить на 30 минут

// Статы
admin.setHappiness(100)
admin.setFullness(100)

// События кошелька (для тестирования анимаций)
admin.triggerWalletCreated()
admin.triggerTransactionReceived('0.1')

// Справка
admin.help()
```

## Структура проекта

```
src/
├── core/                    # Core Layer кошелька
│   ├── wallet/             # Генерация кошельков
│   ├── blockchain/         # Работа с блокчейном
│   └── storage/            # Шифрование и хранение
├── services/               # Service Layer
│   ├── wallet-service.ts   # Сервис кошелька
│   ├── tamagotchi/         # Сервисы тамагочи
│   │   ├── state/         # Управление состоянием
│   │   ├── storage/       # Хранение данных
│   │   ├── timers/        # Таймеры уменьшения статов
│   │   └── constants/     # Константы (питомцы, предметы)
│   └── event-bus.ts       # Event Bus для связи модулей
└── ui/modules/            # UI Layer
    ├── wallet/            # UI модуль кошелька
    └── tamagotchi/        # UI модуль тамагочи
```

## Технологии

- **React 18** — UI фреймворк
- **TypeScript** — типизация
- **Vite** — сборщик и dev-сервер
- **ethers.js** — работа с Ethereum
- **@scure/bip39** — генерация мнемоники
- **crypto-js** — шифрование данных
- **axios** — HTTP клиент для Etherscan API

## Документация

- [WALLET_MODULE.md](./WALLET_MODULE.md) — подробная документация модуля кошелька
- [TAMAGOTCHI_MODULE.md](./TAMAGOTCHI_MODULE.md) — подробная документация модуля тамагочи

## Безопасность

- Приватные ключи и мнемоника шифруются AES-256 перед сохранением
- Пароль не сохраняется, используется только для шифрования/расшифровки
- Данные хранятся локально в `localStorage` браузера
- Проект работает с тестовой сетью Sepolia (не используйте реальные средства)

---

**Примечание:** Это учебный проект, демонстрирующий концепцию модульного криптокошелька. Не используйте для хранения реальных средств.

