# 🏗 Архитектура проекта — Tamagotchi Wallet

## 📐 Общая структура

```
blockchain_project/
├── src/
│   ├── core/                    # 🔐 Бэкенд криптокошелька
│   │   ├── wallet/              # Логика кошелька
│   │   ├── blockchain/          # Работа с блокчейном
│   │   └── storage/             # Хранение данных
│   │
│   ├── services/                # 🔌 Сервисы (связь между слоями)
│   │   ├── wallet-service.ts    # Сервис кошелька
│   │   ├── tamagotchi-service.ts # Сервис тамагочи
│   │   └── event-bus.ts         # Event Bus для связи модулей
│   │
│   ├── ui/                      # 🎨 Фронтенд
│   │   ├── modules/
│   │   │   ├── wallet/          # UI модуль кошелька
│   │   │   │   ├── WalletModule.tsx
│   │   │   │   ├── components/
│   │   │   │   ├── hooks/
│   │   │   │   └── context/
│   │   │   │
│   │   │   └── tamagotchi/      # UI модуль тамагочи
│   │   │       ├── TamagotchiModule.tsx
│   │   │       ├── components/
│   │   │       ├── hooks/
│   │   │       └── context/
│   │   │
│   │   └── shared/              # Общие UI компоненты
│   │       └── components/
│   │
│   ├── tamagotchi/              # 🐾 Логика тамагочи (не UI)
│   │   ├── pet/
│   │   ├── shop/
│   │   └── storage/
│   │
│   └── utils/                   # 🛠 Утилиты
│
├── public/                       # Статические файлы
└── package.json
```

---

## 🔄 Потоки данных

### 1. Кошелёк (бэкенд → фронтенд)

```
Core (wallet) 
  ↓ использует
Services (wallet-service)
  ↓ использует
UI Module (WalletModule)
  ↓ отображает
Компоненты кошелька
```

### 2. Тамагочи (логика → UI)

```
Tamagotchi Logic (pet, shop)
  ↓ использует
Services (tamagotchi-service)
  ↓ использует
UI Module (TamagotchiModule)
  ↓ отображает
Компоненты тамагочи
```

### 3. Связь между модулями

```
WalletModule
  ↓ отправляет события
Event Bus
  ↓ слушает
TamagotchiModule
  ↓ реагирует (награды)
```

---

## 📦 Модули

### WalletModule (UI кошелька)

**Зависимости:**
- `wallet-service` (сервис кошелька)
- `core/wallet` (бэкенд логика)

**Ответственность:**
- Отображение баланса
- Отправка/получение транзакций
- Управление кошельком

**События:**
- `wallet:transaction-sent` → отправляет в Event Bus
- `wallet:balance-changed` → отправляет в Event Bus

---

### TamagotchiModule (UI тамагочи)

**Зависимости:**
- `tamagotchi-service` (сервис тамагочи)
- `tamagotchi/` (логика тамагочи)

**Ответственность:**
- Отображение питомца
- Управление магазином
- Действия с питомцем (кормить, играть)

**События:**
- Слушает `wallet:transaction-sent` → награждает питомца
- Слушает `wallet:balance-changed` → опционально реагирует

---

## 🔌 Сервисы

### wallet-service.ts

**Интерфейс:**
```typescript
interface IWalletService {
  createWallet(): Promise<WalletData>;
  getBalance(address: string): Promise<string>;
  sendTransaction(to: string, amount: string): Promise<TransactionResult>;
  getTransactionHistory(address: string): Promise<Transaction[]>;
}
```

**Реализации:**
- `MockWalletService` — для разработки UI
- `RealWalletService` — настоящая реализация (позже)

---

### tamagotchi-service.ts

**Интерфейс:**
```typescript
interface ITamagotchiService {
  getPet(): Pet;
  feedPet(foodId: string): Promise<ActionResult>;
  playWithPet(activityId: string): Promise<ActionResult>;
  buyItem(itemId: string): Promise<boolean>;
  getShopItems(category: ShopCategory): ShopItem[];
}
```

**Реализация:**
- `TamagotchiService` — работает с логикой тамагочи

---

## 🗄 Хранение данных

### Кошелёк:
- `localStorage` / `IndexedDB`
- Зашифрованные seed-фразы
- История транзакций

### Тамагочи:
- `localStorage` / `IndexedDB`
- Состояние питомца
- Купленные предметы
- Игровая валюта

**Раздельное хранение** — безопасность кошелька не зависит от тамагочи

---

## 🎯 Текущий фокус разработки

**Работаем только с UI тамагочи:**

1. ✅ Структура проекта
2. ⏳ TamagotchiModule (главный компонент)
3. ⏳ Страницы: Home, Shop, Entertainment, Food
4. ⏳ Сервис тамагочи (mock данные)
5. ⏳ Компоненты тамагочи

**Кошелёк и бэкенд:**
- Пока не трогаем
- Будет реализован позже
- UI кошелька будет использовать mock сервис

---

## 📝 Принципы архитектуры

1. **Разделение ответственности** — каждый модуль делает своё
2. **Независимость модулей** — можно разрабатывать отдельно
3. **Слабая связь** — связь через события/интерфейсы
4. **Легко тестировать** — каждый слой отдельно
5. **Масштабируемость** — легко добавлять новые функции


